// Coefficients for the pseudorandom sequence tile display order
export const ROW_DELAY_COEF = 7;
export const COL_DELAY_COEF = 11;

export const HERO_ROWS = 6;
export const HERO_COLS = 8;
export const HERO_ACCENTS = new Set<number>([5, 17, 30, 35]);

export const TILE_WIDTH = 100 / HERO_COLS;
export const TILE_HEIGHT = 100 / HERO_ROWS;
